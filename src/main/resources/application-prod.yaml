server:
  port: 80
wx:
  # 服务区回调需要
  token: 222

  # 用户信息
  user:
    appid: xxx
    secret: xxx
    grant_type: client_credential

spring:
  datasource:
    url: jdbc:mysql://xxx:3306/dingyuehao?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: xxx
    password: xxx

  redis:
    host: xxx
    port: 6379
    password: xxx

logging:
  level:
    org:
      springframework: error
    com.wanfeng.dingyuehao: error

image:
  path: "/root/image/"